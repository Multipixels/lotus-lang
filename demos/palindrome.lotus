-> Returns whether a string is a palindrome.
boolean(string s) isPalindrome {
	integer i = 0;
	integer j = s.length - 1;

	while(i < j) {
		if(s[i] != s[j]) {
			return false;
		}

		i++;
		j--;
	}

	return true;
}

log(isPalindrome("racecar"));